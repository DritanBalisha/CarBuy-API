
.___________________________________________.
|           drialbXauto CRUD-API            |
|-------------------------------------------|



Functions :
JWT authentication -> user register and login, password is hashed and stored in db,
Create, Delete, Update an car pOst -> User can login and post his car, he can edit, delete it.
                                      user can see all his posted cars.
CarPost filter -> ask the user if he knows what car to buy or not, if not user completes some,
                  question about the car, like: feul, gearbox, vehicleType and gives all,
                  machting results.
Admin User -> has accsses to a special url that give's him all users info(pass is hashed),
              admin user can delete other users or make changes


DB connection -> MySql 

Project made by Dritan Balisha .

